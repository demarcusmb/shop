<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Merchandise Store Page</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <header>
        <h1>List of Merchandise</h1>
        <nav>
            <ul>
                <li>
                    <a href="/">Home</a>
                </li>
                <% for(let i=0; i < merch.length; i++){ %>
                    <li>
                        <a href="/merch/<%= merch[i]._id %>">
                            <%= merch[i].name %>
                        </a>
                    </li>
                    <% } %>
                    <li>
                        <a href="/merch/new">Add a New Merchandise</a>
                    </li>
            </ul>
        </nav>
    </header>
</body>

</html>